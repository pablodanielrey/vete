version: "3.5"
services:
  db_login:
    image: postgres
    ports:
      - 5432:5432/tcp
    volumes:
      - /tmp/vete_db:/var/lib/postgresql/data
    environment:
      POSTGRES_DB: vete
      POSTGRES_USER: vete
      POSTGRES_PASSWORD: vete
    #network_mode: "host"
